version: v2
modules:
  - path: .
lint:
  use:
    - STANDARD
  except:
    # Enum value names ARE the protocol wire vocabulary (e.g. OPEN, FULL_LOCKED),
    # so we intentionally do NOT prefix them with the enum name.
    - ENUM_VALUE_PREFIX
    - ENUM_ZERO_VALUE_SUFFIX
breaking:
  use:
    - FILE
